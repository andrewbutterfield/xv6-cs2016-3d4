#include "types.h"
#include "defs.h"
#include "proc.h"
#include <stdio.h>

void swtch(struct context **old, struct context *new){

   printf("Switching to running process %d\n",(int)(c->proc));
   p->state = RUNNABLE;

}
